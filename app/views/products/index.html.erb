<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice">
      <%= notice %>
    </p>
  <% end %>

  <% content_for :title, "The Best Products" %>

  <!-- Centering the Heading -->
  <div class="flex justify-center items-center mb-6">
    <h1 class="font-bold text-4xl text-center font-brokenbrush">The Best Products</h1>
  </div>

  <!-- Centering the Product Grid and making it responsive -->
  <div id="products" class="min-w-full grid grid-cols-1 md:grid-cols-4 gap-8 justify-items-center">
    <% @products.each do |product| %>
      <%= render product %>
    <% end %>
  </div>

  <div class="mt-4 w-full">
    <%= link_to "Shop by Category", categories_path, class: "rounded-lg h-80 w-full bg-gray-200 text-2xl font-semibold p-2" %>
  </div>
  <div class="mt-4 w-full">
    <%= link_to "Create New Product", new_product_path, class: "rounded-lg py-3 px-5 bg-blue-500 text-white text-2xl font-semibold" %>
  </div>
</div>
