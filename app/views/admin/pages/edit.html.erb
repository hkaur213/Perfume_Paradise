<%= form_with(model: @about_page, url: admin_about_path, method: :patch) do |form| %>
    <div class="field">
      <%= form.label :content, "About Page Content" %>
      <%= form.rich_text_area :content %>
    </div>
    <%= form.submit "Update Page", class: "btn btn-primary" %>
  <% end %>
  